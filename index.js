//pop up message that will be displayed regardless of the number
const popupMessage = `You have received this message because you have been chosen to open an important vault. Here is the secret combination:`;

//first number will add modulo to 10
const firstNum = 40 % 30;

//second number will subtract down to 40

const secondNum = 45 - 5;

//third number will add to 39

const thirdNum = 12 + 27;

alert(`${popupMessage} ${firstNum} - ${secondNum} - ${thirdNum}`);
